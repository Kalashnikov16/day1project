# Used by Prisma CLI (Replace with your actual MySQL credentials)
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/day1project"

# Used by the MariaDB driver in the controller/migration scripts
DB_HOST="localhost"
DB_USER="root"
DB_PASSWORD=""
DB_NAME="day1project"